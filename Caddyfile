# Замените n8n.example.com на реальный домен из .env (N8N_HOST).
# Перед первым запуском домен должен указывать A-записью на IP сервера,
# иначе Let's Encrypt не удастся получить сертификат.

n8n.example.com {
    reverse_proxy n8n:5678
}
